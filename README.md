# JPEG encoder/decoder

A JPEG-like encoder/decoder that takes PPM images as input, performs the JPEG compression algorithm and returns the decoded PPM image.

Performs quantization, discrete cosing transform and huffman encoding in the process.